# Simple Python Practice - Caesar Cipher
# Encrypting white space as well
Alphabet = " ABCDEFGHIJKLMNOPQRSTUVWXYZ"

# Pick a key
KEY = 12


def caesar_encrypt(plain_text):
    cipher_text = ''
    plain_text = plain_text.upper()
    for char in plain_text:
        idx = Alphabet.find(char)
        idx = (idx + KEY) % len(Alphabet)
        cipher_text += Alphabet[idx]

    return cipher_text


def caesar_decrypt(cipher_text):
    plain_text = ''
    cipher_text = cipher_text.upper()
    for char in cipher_text:
        idx = Alphabet.find(char)
        idx = (idx - KEY) % len(Alphabet)
        plain_text += Alphabet[idx]

    return plain_text

# Cracking Caesar Cipher with brute Force lol
def caesar_crack(cipher):
    for key in range(len(ALPHABET)):

        plain_text = ''
        for c in cipher:
            idx = ALPHABET.find(c)
            idx = (idx - key) % len(ALPHABET)
            plain_text += ALPHABET[idx]
        print('with key %s, the message is: %s'%(key, plain_text))



name = 'Thomas Van De Crommenacker'
secret = caesar_encrypt(name)
print(secret)
print(caesar_decrypt(secret))
